namespace ItsLeet.Graph;

public class MinimumTimeToReachLastRoom
{
    // TODO: Complete this challenge
    public int MinTimeToReach(int[][] moveTime) {
        var minTime = Int32.MaxValue;
        for (var i = 0; i < moveTime.Length; i++)
        {
            var sumPos = moveTime[i][0];
            for (var j = 0; j < moveTime[i].Length; j++)
            {
                
            }
        }
        
        return minTime;
    }
}